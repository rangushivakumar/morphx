export function productDescription(name: string, context: string): string {
  return `Bespoke ${name} engineered for commercial ${context} — premium materials, custom dimensions, and manufacturer-direct pricing.`
}
